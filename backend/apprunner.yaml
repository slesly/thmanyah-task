version: 1.0
runtime: nodejs18

build:
  commands:
    pre-build:
      - echo "Installing dependencies..."
      - npm ci
      - echo "Current directory:"
      - pwd
      - echo "Directory contents:"
      - ls -la
    build:
      - echo "Building application..."
      - npm run build
      - echo "Checking dist folder..."
      - ls -la dist/
      - echo "Checking if main.js exists:"
      - ls -la dist/main.js || echo "main.js not found"
      - echo "Checking if main.js.map exists:"
      - ls -la dist/main.js.map || echo "main.js.map not found"

run:
  command: node dist/main.js
  network:
    port: 3001
  env:
    - name: NODE_ENV
      value: production
    - name: PORT
      value: "3001"
    - name: DB_HOST
      value: "thmanyah-db-1.cspi46cem801.us-east-1.rds.amazonaws.com"
    - name: DB_PORT
      value: "5432"
    - name: DB_USERNAME
      value: "postgres"
    - name: DB_PASSWORD
      value: "P5t6d8f10"
    - name: DB_DATABASE
      value: "thmanyah-db-1"
    - name: ALLOWED_ORIGINS
      value: "http://localhost:3000,https://main.d19e30r7ro0wel.amplifyapp.com,https://agd9mkwapi.us-east-1.awsapprunner.com"
